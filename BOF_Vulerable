#include <stdio.h>
#include <string.h>
int arg_copy(char *what_to){
        char buffer[20];
        strcpy(buffer,what_to);
        printf("%s",buffer);
}
int main(int argc,char **argv){
        arg_copy(argv[1]);
}

